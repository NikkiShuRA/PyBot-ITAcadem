# PyBot — бот для ITAcadem

Я — разработчик этого проекта. Здесь — короткое, честное и понятное описание только того, что уже реализовано в кодовой базе.

🌟 Кратко

- PyBot — асинхронный Telegram‑бот на Python.
- Используемые библиотеки (в проекте сейчас): `aiogram`, `aiogram_dialog`, `SQLAlchemy`, `asyncpg`, `python-dotenv`.
- Точка входа: `run.py` (запускает `tg_bot.tg_bot_main`).

Ссылка на репозиторий: https://github.com/NikkiShuRA/PyBot-ITAcadem

---

🚀 Быстрый старт (локально, PowerShell)

1. Клонировать репозиторий и перейти в папку проекта:

```powershell
git clone https://github.com/NikkiShuRA/PyBot-ITAcadem.git
cd PyBot
```

2. Создать и активировать виртуальное окружение:

```powershell
python -m venv .venv
. .\.venv\Scripts\Activate.ps1
```

3. Установить зависимости:

```powershell
pip install -r requirements.txt
```

4. Создать `.env` на основе `.env.example` и заполнить реальные значения (токен бота, параметры БД):

```powershell
copy .env.example .env
# отредактируйте .env
```

5. Запустить бота:

```powershell
python run.py
```

---

📁 Что фактически есть в проекте

- `run.py` — точка входа.
- `tg_bot/tg_bot_run.py` — инициализация `Bot`, `Dispatcher`, регистрация роутеров и DialogManager, подключение middleware для сессии БД.
- `tg_bot/handlers/common/start.py` — обработчик `/start` и приём контакта для авторизации.
- `tg_bot/handlers/user/profile_dialog.py` — многошаговый диалог создания профиля (`aiogram_dialog`).
- `db_class/database.py` — создание асинхронного движка, `SessionLocal`, `init_db()`.
- `db_class/models/` — SQLAlchemy модели для пользователей, проектов и задач.
- `services/` — бизнес‑логика (напр., `services/users.py`).

---

🔧 Как это работает (коротко)

- При старте вызывается `init_db()` — если соединение с БД доступно, таблицы создаются.
- В `tg_bot/tg_bot_run.py` есть простой middleware, который создаёт сессию `SessionLocal` и пробрасывает её в обработчики как `db`.
- При отправке контакта бот ищет пользователя по номеру; если профиль не найден — запускается диалог создания профиля.

---

🔑 Переменные окружения (важные)

- `BOT_TOKEN_TEST` — токен тестового бота (в `config.py` читается как `BOT_TOKEN`).
- `DB_USER`, `DB_PASS`, `DB_HOST`, `DB_PORT`, `DB_NAME` — параметры подключения к PostgreSQL.

⚠️ Не храните реальные токены в репозитории. Для CI используйте GitHub Secrets.

---

📋 Команды бота (реализованные обработчики)

- `/start` — начало работы, предложение авторизации (кнопка для отправки контакта).
- `/help` — список команд.
- `/info` — информация о боте.

---

💡 Заметки от разработчика

- README отражает текущее состояние кода — ничего лишнего.

---

© 2025 — NikkiShuRA

