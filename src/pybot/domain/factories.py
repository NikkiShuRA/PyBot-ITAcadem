from typing import TYPE_CHECKING

if TYPE_CHECKING:
    from ..domain import Points, LevelSystemEntity

# Недооценённый гений

# def default_academic_points() -> Points:
#     """Дефолтные академические баллы (value=0, type=ACADEMIC)."""
#     return Points(value=0, point_type=LevelSystemEntity.ACADEMIC)

# def default_reputation_points() -> Points:
#     """Дефолтные репутационные баллы (value=0, type=REPUTATION)."""
#     return Points(value=0, point_type=LevelSystemEntity.REPUTATION)
